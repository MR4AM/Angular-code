import { EchartsDirective } from './echarts.directive';

describe('EchartsDirective', () => {
  it('should create an instance', () => {
    const directive = new EchartsDirective();
    expect(directive).toBeTruthy();
  });
});
